/**
 * Complete the setLeapYear method to properly detect leap years.
 */
public class LeapYear {
    // write this:
    // return true if year is divisible by 4, unless it is
    // also divisible by 100 and not divisible by 400.
    //(for example, 1900 → divisible by 100 but not 400 → not a leap year)
    // Otherwise return false

    public boolean isLeapYear(int year) {
       return false;
    }
  }
  